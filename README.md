## Installation

### Option 1 (standard)
pip install -r requirements.txt

### Option 2 (reproducible)
pip install -r requirements.lock

### Option 3 (if network issues occur)
pip install -r requirements.txt \
  --index-url https://pypi.tuna.tsinghua.edu.cn/simple